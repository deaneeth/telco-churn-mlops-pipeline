# Core production dependencies
pandas>=1.5.0
numpy>=1.21.0
scikit-learn==1.6.1  # Pinned to exact version for model compatibility
joblib>=1.2.0
mlflow>=2.0.0

# Orchestration
apache-airflow>=2.5.0

# Development and testing
pytest>=7.0.0
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.0.0
Waitress>=2.1.2

# Optional dependencies (not used in current production pipeline)
# flask
# pyspark
# python-dotenv
# openpyxl